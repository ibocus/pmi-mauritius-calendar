# Fine-grained GitHub PAT with "Issues: write" access on this repo.
# Used by /api/suggestions to file member workshop suggestions as GitHub issues.
GITHUB_TOKEN=

# Optional: override the repo suggestions are filed against (owner/repo).
# Defaults to ibocus/pmi-mauritius-calendar.
SUGGESTIONS_REPO=
